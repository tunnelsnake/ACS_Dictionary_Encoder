#ifndef DICT_H
#define DICT_H

#include <unordered_map>
#include <vector>

class dict {
    public:
        dict() {}
        ~dict() {}

        void insert( std::vector<bool> * bits );
        std::unordered_map<int, locate()
        void extract()

    private:
        std::unordered_map<int, std::pair<std::vector<bool>, int>>
}

#endif // DICT_H